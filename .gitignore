# Secrets – niemals in Git!
.env
backup/ssh/backup_key

# FoundryVTT ZIP – kommerzielle Software, nicht ins Backup (>100MB)
*.zip

# Docker Build Cache
.dockerignore

# User-Daten vom Live-Host – werden in foundryvtt-backups gesichert, nicht hier!
/data/

# Lokale Migrationssicherungen (enthalten Daten und Secrets)
/migration-*/
