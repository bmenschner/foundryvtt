# Private SSH Key – darf NIEMALS in Git!
backup/ssh/backup_key

# FoundryVTT ZIP – kommerzielle Software, nicht ins Backup (>100MB)
*.zip

# Docker Build Cache
.dockerignore

# User-Daten vom Live-Host – werden in foundryvtt-backups gesichert, nicht hier!
data/

# SSL-Zertifikate – laufzeitgeneriert, bei Verlust via init-letsencrypt.sh neu ausstellen
certbot/
